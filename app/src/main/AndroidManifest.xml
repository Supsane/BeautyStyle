<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chashurinevgeny.beautystyle">

    <application
        android:allowBackup="true"
        android:icon="@drawable/style_logo"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/appName">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ServicesCategoryActivity" />
        <activity android:name=".AdressActivity" />
        <activity android:name=".ServicesActivity" />
        <activity android:name=".ActivityForFragment"></activity>
    </application>

</manifest>